%nav.navbar-inverse
  %ul.nav.side-nav-rigth
    - if !@room_users.nil?
      - @room_users.each do |user|
        %li.joined_friend{:data_user_id => user.id, :data_room_id => @room.id}
          = "#{user.login}"
          %span.glyphicon.glyphicon-minus.pull-right

